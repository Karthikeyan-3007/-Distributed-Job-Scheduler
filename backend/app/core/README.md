Reserved for cross-cutting core utilities not tied to one layer (e.g. custom
exception types, shared constants). Currently empty in this MVP; error
handling lives in `app/middleware`, business rules in `app/services`.
