Reserved for a future event-driven execution model (e.g. publishing
JobCompleted/JobFailed domain events for downstream consumers or webhooks).
Not required for the current polling-based worker design — see the
"What's intentionally out of scope" section of the root README.
